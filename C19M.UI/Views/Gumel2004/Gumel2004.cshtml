@model C19M.UI.Models.Gumel2004

@{
    ViewBag.Title = "Gumel2004";
}

<head>
    <title>Line Chart</title>
    <script src="~/Scripts/Chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/luxon@1.15.0"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-luxon@0.2.0"></script>
    <script src="~/Scripts/utils.js"></script>
    <style>
        canvas {
            -moz-user-select: none;
            -webkit-user-select: none;
            -ms-user-select: none;
        }
    </style>
</head>

<div>
    <canvas id="canvas"></canvas>
</div>
<script>
    function HongKong_GenerateData() {

        var HongKong_Days = @Html.Raw(Json.Encode(Model.Days));

        var HongKong_DayCumulativeDiseaseInducedDeaths = @Html.Raw(Json.Encode(Model.HongKong_DayCumulativeDiseaseInducedDeaths_Array));

        var HongKong_Data = [];

        var HongKong_i = 0;

        for (; HongKong_i < HongKong_Days.length; HongKong_i++) {
            HongKong_Data.push(
                {
                    x: luxon.DateTime.fromFormat(HongKong_Days[HongKong_i], 'LLL dd yyyy').valueOf(),
                    y: HongKong_DayCumulativeDiseaseInducedDeaths[HongKong_i]
                });
        }

        return HongKong_Data;
    }

    function GreaterTorontoArea_GenerateData() {

        var GreaterTorontoArea_Days = @Html.Raw(Json.Encode(Model.Days));

        var GreaterTorontoArea_DayCumulativeDiseaseInducedDeaths = @Html.Raw(Json.Encode(Model.GreaterTorontoArea_DayCumulativeDiseaseInducedDeaths_Array));

        var GreaterTorontoArea_Data = [];

        var GreaterTorontoArea_i = 0;

        for (; GreaterTorontoArea_i < GreaterTorontoArea_Days.length; GreaterTorontoArea_i++) {
            GreaterTorontoArea_Data.push(
                {
                    x: luxon.DateTime.fromFormat(days[GreaterTorontoArea_i], 'LLL dd yyyy').valueOf(),
                    y: GreaterTorontoArea_DayCumulativeDiseaseInducedDeaths[GreaterTorontoArea_i]
                });
        }

        return GreaterTorontoArea_Data;
    }

    const ctx = document.getElementById('canvas').getContext('2d');
    ctx.canvas.width = 1000;
    ctx.canvas.height = 300;

    const color = Chart.helpers.color;
    const cfg = {
        data: {
            datasets: [{
                label: 'Hong Kong',
                backgroundColor: color(window.chartColors.red).alpha(0.5).rgbString(),
                borderColor: window.chartColors.red,
                data: HongKong_GenerateData(),
                type: 'line',
                pointRadius: 0,
                fill: false,
                lineTension: 0,
                borderWidth: 2
            }]
        },
        options: {
            animation: {
                duration: 0
            },
            scales: {
                x: {
                    type: 'time',
                    distribution: 'series',
                    offset: true,
                    ticks: {
                        major: {
                            enabled: true,
                        },
                        fontStyle: 'bold',
                        source: 'data',
                        autoSkip: true,
                        autoSkipPadding: 75,
                        maxRotation: 0,
                        sampleSize: 200
                    },
                },
                y: {
                    type: 'linear',
                    gridLines: {
                        drawBorder: false
                    },
                    scaleLabel: {
                        display: true,
                        labelString: 'Cumulative Number of Disease-Induced Deaths'
                    }
                }
            },
            tooltips: {
                intersect: false,
                mode: 'index',
                callbacks: {
                    label: function (tooltipItem, myData) {
                        let label = myData.datasets[tooltipItem.datasetIndex].label || '';
                        if (label) {
                            label += ': ';
                        }
                        label += parseFloat(tooltipItem.value).toFixed(2);
                        return label;
                    }
                }
            }
        }
    };

    const chart = new Chart(ctx, cfg);
</script>